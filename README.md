# homebrew-clutch

Homebrew tap for [Clutch](https://github.com/fancy1108/Clutch) (Apple Silicon macOS).

```bash
brew tap fancy1108/clutch
brew install --cask clutch
```

Unsigned DMG — the cask runs `xattr -cr` after install. Intel Mac is not supported.
